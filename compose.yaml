# No daemon yet: every command is one-shot, so the verb is
#   docker compose run --rm orrery <command>
# `docker compose up` is not useful until a long-running service exists.
services:
  orrery:
    build: .
    environment:
      ORRERY_DATA_DIR: /data   # always the mount below, whatever .env says
    volumes:
      - ./data:/data
      - ./.env:/app/.env       # settings; the app's Setup tab writes it in place (touch .env first)
    extra_hosts:
      - "host.docker.internal:host-gateway"   # a host Ollama, for ORRERY_EMBED_BASE_URL
